# =========================================================================
# === .gitignore for MMDetection Project ===
# =========================================================================

# 1. 忽略所有数据文件
# 数据集是项目的依赖，但不应成为代码库的一部分。
# README中应指导用户如何自行下载和准备数据。
data/

# 2. 忽略所有实验输出
# 日志、模型权重等都是运行代码后生成的，不应上传。
# 最终的、需要分享的模型权重将使用Git LFS单独处理。
work_dirs/
output/

# 新增：忽略模型检查点文件
checkpoints/

# 3. 忽略Python虚拟环境
# 每个开发者都应该自行创建虚拟环境。
# 根据你的环境名称修改，例如 venv/, .venv/, env/ 等
openmmlab/

# 4. 忽略Python编译和缓存文件
# 这些文件是自动生成的，并且是平台相关的。
__pycache__/
*.pyc
*.pyo
*.pyd

# 5. 忽略IDE和编辑器的配置文件
# 保持代码库的编辑器无关性。
.vscode/
.idea/
*.swp
*.swo

# 6. 忽略操作系统生成的文件
.DS_Store
Thumbs.db

# 新增：忽略 MMDetection 源码文件夹
mmdetection/